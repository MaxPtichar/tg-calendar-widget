[package]
name = "tg-calendar-widget"
version = "0.1.0"
edition = "2021"
readme = "README.md"
license = "MIT OR Apache-2.0"

[dependencies]
chrono = "0.4.38"
teloxide = { version = "0.13.0", optional = true } 

[features]
default = []
teloxide = ["dep:teloxide"]